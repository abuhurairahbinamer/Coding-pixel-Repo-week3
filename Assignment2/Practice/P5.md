Using useEffect(() => { load() }, []) ensures the fetch runs only once when the component mounts. If data is added to the dependency array, it causes an infinite loop because the effect fetches data and updates data, which then triggers the effect again repeatedly, leading to continuous API calls.

//Deeper

 AbortController is used to cancel an ongoing fetch when the component unmounts, preventing memory leaks and unwanted state updates.